// use crate::arrays::largest_element::solve;
// use crate::arrays::second_largest_element::solve;
// use crate::arrays::check_sorted::solve;
// use crate::arrays::linear_search::solve;

// use crate::binary_search::koko_eat::min_eating_speed;
// use crate::binary_search::bouquets::min_days;

pub mod arrays;
pub mod binary_search;
pub mod recursion;

fn main() {
    println!("Hello World.");
    // let arr = vec![2, 5, 1, 3, 0];
    // let largest = solve(&arr);
    // println!("The largest element is: {}", largest);

    // let arr = vec![1];
    // let (sl, sm) = solve(&arr);
    // println!("Second largest : {}\nSecond smallest : {}", sl, sm);

    // let arr = vec![1, 2, 3, 4, 5];
    // println!("Array is sorted ? {}", solve(&arr));

    // let arr = vec![5, 4, 3, 2, 1];
    // let n = 2;
    // println!("Index of {} is {}", n, solve(&arr, n));

    // let arr = vec![30, 11, 23, 4, 20];
    // let h = 6;
    // println!("Minimum Speed : {}", min_eating_speed(arr, h));

    // let bloom_day = vec![1, 10, 3, 10, 2];
    // let m = 3;
    // let k = 1;
    // println!("Minimum no. of days = {}", min_days(&bloom_day, m, k));
}
